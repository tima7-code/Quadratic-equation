#pragma once
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

float Solution_zero_C(float * coefficient);
float Solution(float* coefficient, float discriminant);
float Discriminant(float* coefficient);
float Solution_linear_education(float* coefficient);